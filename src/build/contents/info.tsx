export const NavInformation = () => {
    /* 
        Erklären, wie die Navbar funktioniert. Die Icons darstellen (icons importieren, siehe imports in nav.tsx)
        Erklären, was die Icons machen (siehe Code in nav.tsx)
        Halt so, dass es schön ist.
        Verwende css code, Infos dafür im Github (Commit e21e0fe)
        Im css KEINE PX verwenden (also Pixelanzahl), es wird alles mit % gemacht.
     */
    return (
        <p>NavInformation</p>
    );
}

export const GeneralInformation = () => {
    return (
        <p>General Information</p>
    );
}