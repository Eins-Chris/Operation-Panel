const UserSelection = () => {
    /* 7 Fenster mit auswählbaren Usern */
    /* 7 Fenster mit auswählbaren bestehenden Sessions */
    /* Ausgegraut, was nicht möglich ist (wenn für user keine Session besteht, Feld leer) */
    /* durch onClick={() => setUserSelected(true)} wird in die app geschalten */

    console.log()

    return (
        <>
            <p>User Selection Buttons</p>    
        </>
    );
}

export default UserSelection;
